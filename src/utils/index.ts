// utils/index.ts
// ─── Utility Functions ────────────────────────────────────────────────────────
// Pure helper functions only — no side effects, no HTTP, no DB access.
// Examples: date formatting, string transformations, math helpers.
//
// Example usage:
//   export function formatDate(date: Date): string { ... }
